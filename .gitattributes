# Normalize line endings and text handling

# Go source files use LF on all platforms
*.go text eol=lf

# Plain text files use LF
*.txt text eol=lf

# Let Git auto-detect text/binary for everything else
* text=auto

